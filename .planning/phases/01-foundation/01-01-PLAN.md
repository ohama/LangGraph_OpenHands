---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env
  - .gitignore
  - orchestrator/__init__.py
  - orchestrator/graph/__init__.py
  - orchestrator/graph/state.py
  - orchestrator/graph/stub_graph.py
  - orchestrator/persistence/__init__.py
  - orchestrator/persistence/job_store.py
  - tests/__init__.py
  - tests/test_job_store.py
  - tests/test_stub_graph.py
autonomous: true

must_haves:
  truths:
    - "OrchestratorState carries typed string fields (goal, job_id, research_findings, plan, execution_result, execution_status) plus an append-only event_log — NO unbounded messages accumulator (ORCH-04)"
    - "A stub LangGraph graph with nodes named research/plan/execute compiles with AsyncSqliteSaver and runs research->plan->execute to END"
    - "Graph state is checkpointed per node to checkpoints.db keyed by thread_id==job_id, and the checkpoint survives reopening the DB file (PERSIST-01)"
    - "jobs.db has a jobs table tracking id/goal/status/result/error/created_at/updated_at with WAL mode (PERSIST-02)"
    - "EMPIRICAL: resuming an existing thread_id with None (vs initial_state) is tested with langgraph==1.2.6 and the correct pattern is recorded"
    - "EMPIRICAL: astream(stream_mode='updates') chunk key equals the node name string, confirmed by printing a raw chunk"
  artifacts:
    - path: "orchestrator/graph/state.py"
      provides: "OrchestratorState TypedDict (the REAL schema for Phases 2/3)"
      contains: "class OrchestratorState"
    - path: "orchestrator/graph/stub_graph.py"
      provides: "build_stub_graph() with research/plan/execute nodes"
      contains: "def build_stub_graph"
    - path: "orchestrator/persistence/job_store.py"
      provides: "JobStore aiosqlite CRUD + init_jobs_db schema"
      contains: "class JobStore"
    - path: "pyproject.toml"
      provides: "Pinned Phase 1 dependencies"
      contains: "langgraph"
    - path: "tests/test_stub_graph.py"
      provides: "Checkpoint-survives-reopen test + two empirical-verification tests"
      contains: "AsyncSqliteSaver"
  key_links:
    - from: "orchestrator/graph/stub_graph.py"
      to: "orchestrator/graph/state.py"
      via: "StateGraph(OrchestratorState)"
      pattern: "StateGraph\\(OrchestratorState\\)"
    - from: "tests/test_stub_graph.py"
      to: "checkpoints.db"
      via: "AsyncSqliteSaver.from_conn_string reopened to verify persistence"
      pattern: "from_conn_string"
    - from: "orchestrator/persistence/job_store.py"
      to: "jobs.db"
      via: "PRAGMA journal_mode=WAL on every connection"
      pattern: "journal_mode=WAL"
---

<objective>
Build the durable data foundation for the orchestrator: the REAL graph state schema, the stub LangGraph graph (research/plan/execute nodes), and the aiosqlite job registry — all proven to checkpoint and survive a DB reopen, with the two research-flagged behaviors empirically verified.

Purpose: Every later phase (LLM nodes, OpenHands execute, resume-on-restart) slots into this exact state schema and graph shape without renaming fields. This plan proves persistence works BEFORE any app server or LLM exists.
Output: pyproject.toml with pinned deps, orchestrator package scaffold, OrchestratorState, build_stub_graph(), JobStore + init_jobs_db, and a passing test suite including the two empirical-verification tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

# 01-RESEARCH.md is PRIMARY. It contains version-pinned deps and verified code-shape
# snippets for every file in this plan (Patterns 2, 3, 5; Code Examples section).
# Use those snippets as the authoritative starting point.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold + pinned dependencies (uv, Python 3.12)</name>
  <files>pyproject.toml, .env, .gitignore, orchestrator/__init__.py, orchestrator/graph/__init__.py, orchestrator/persistence/__init__.py, tests/__init__.py</files>
  <action>
Create the uv-managed Python 3.12 project skeleton per RESEARCH.md "Recommended Project Structure" and "Installation".

1. Create `pyproject.toml` with project name `orchestrator`, requires-python ">=3.12", and these EXACT pinned dependencies (from RESEARCH.md Standard Stack — do not float versions):
   - `langgraph==1.2.6`
   - `langgraph-checkpoint-sqlite==3.1.0`  (MUST be >=3.0.1 — CVE-2025-67644)
   - `fastapi==0.137.2`
   - `uvicorn[standard]==0.49.0`
   - `aiosqlite>=0.19`
   - `pydantic>=2.7`
   - `python-dotenv>=1.0`
   - dev deps: `pytest>=8.0`, `pytest-asyncio>=0.24`, `ruff`
   Configure pytest-asyncio in `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` so `async def test_*` works without per-test markers.
2. Create the package directories with empty `__init__.py`: `orchestrator/`, `orchestrator/graph/`, `orchestrator/persistence/`, `tests/`. (worker/ and api/ packages are created in 01-02 and 01-03.)
3. Create `.env` with `DATA_DIR=./data` and `LOG_DIR=./logs`.
4. Create `.gitignore` covering `.venv/`, `data/`, `logs/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`. (Do NOT commit the SQLite DB files or per-run logs.)
5. Create the runtime dirs: `mkdir -p data logs`.
6. Create the venv and install: `uv venv .venv --python 3.12` then `uv pip install -e ".[dev]"` (or install the explicit list from RESEARCH.md if the editable extras path is fiddly). Use `uv`, never bare `pip`.

Do NOT add langchain-openai or openhands-sdk — those are Phase 2/3 deps.
  </action>
  <verify>
`uv run python -c "import langgraph, langgraph.checkpoint.sqlite.aio, fastapi, aiosqlite, pydantic; print('imports ok')"` prints "imports ok".
`uv run python -c "import langgraph; print(langgraph.__version__)"` prints `1.2.6`.
`uv run pytest --collect-only` runs without import errors (zero tests collected is fine at this point).
  </verify>
  <done>pyproject.toml pins the 7 runtime deps at the RESEARCH.md versions; the orchestrator/graph and orchestrator/persistence packages import cleanly; data/ and logs/ exist; .venv is created via uv with langgraph 1.2.6.</done>
</task>

<task type="auto">
  <name>Task 2: OrchestratorState + stub graph + JobStore (jobs.db)</name>
  <files>orchestrator/graph/state.py, orchestrator/graph/stub_graph.py, orchestrator/persistence/job_store.py</files>
  <action>
Implement the three foundation modules using the verified snippets in RESEARCH.md Patterns 2, 3, and 5 (and the "Minimal Init for jobs.db" code example).

1. `orchestrator/graph/state.py` — `OrchestratorState(TypedDict)` exactly per RESEARCH.md Pattern 2:
   - Immutable: `goal: str`, `job_id: str`
   - Optional single-write fields: `research_findings: Optional[str]`, `plan: Optional[str]`, `execution_result: Optional[str]`, `execution_status: Optional[str]`
   - `event_log: Annotated[list[str], operator.add]` (append-only audit trail)
   - CRITICAL (ORCH-04): NO `messages` field, NO conversation accumulator. Typed string fields only.

2. `orchestrator/graph/stub_graph.py` — per RESEARCH.md Pattern 3:
   - Async stub nodes `research_stub` (sleep 1.0s), `plan_stub` (sleep 1.0s), `execute_stub` (sleep 2.0s). Each returns ONLY the keys it changes (partial dict) plus one ISO-8601 UTC `event_log` entry.
   - `execute_stub` sets `execution_result="STUB: execution complete"` and `execution_status="success"`.
   - `build_stub_graph() -> StateGraph`: nodes added with names "research", "plan", "execute" (these are the REAL names Phase 2/3 reuse); entry point "research"; edges research->plan->execute->END. Return the UNcompiled builder (compilation with the checkpointer happens in 01-02 lifespan and in tests).

3. `orchestrator/persistence/job_store.py` — per RESEARCH.md Pattern 5 + Code Example:
   - `async def init_jobs_db(db_path)` creating the `jobs` table (id PK, goal, status DEFAULT 'PENDING', result, error, created_at, updated_at) with `PRAGMA journal_mode=WAL`. Idempotent (IF NOT EXISTS).
   - `class JobStore` with `_connect()` setting `PRAGMA journal_mode=WAL` + `PRAGMA synchronous=NORMAL` on every connection, and methods: `create_job(job_id, goal)`, `set_status(job_id, status)`, `set_complete(job_id, result)`, `set_failed(job_id, error)`, `get_job(job_id) -> dict|None`, `get_jobs_by_status(status) -> list[dict]`. Use ISO-8601 UTC for created_at/updated_at.

Honor the hard constraints: WAL on every connection; jobs.db is app-owned (never mixed with checkpoints.db); the worker is the only writer (these methods are written here, called in 01-02).
  </action>
  <verify>
`uv run python -c "from orchestrator.graph.state import OrchestratorState; from orchestrator.graph.stub_graph import build_stub_graph; build_stub_graph(); print('graph builds')"` prints "graph builds".
`uv run python -c "import inspect; from orchestrator.graph import state; assert 'messages' not in inspect.getsource(state), 'ORCH-04 violated: messages accumulator present'; print('ORCH-04 ok')"`.
`uv run pytest tests/test_job_store.py -q` passes (test written in Task 3).
  </verify>
  <done>OrchestratorState has the 6 typed fields + event_log and no messages accumulator; build_stub_graph() returns a graph with nodes research/plan/execute wired research->plan->execute->END; JobStore + init_jobs_db create the WAL-mode jobs table and provide full CRUD.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — checkpoint persistence + two EMPIRICAL verifications</name>
  <files>tests/test_job_store.py, tests/test_stub_graph.py</files>
  <action>
Write pytest-asyncio tests proving persistence and resolving the two research-flagged empirical questions. Use `tempfile`-backed DB paths so tests are isolated.

1. `tests/test_job_store.py`:
   - Test create_job then get_job returns status PENDING.
   - Test set_status -> get_job reflects new status; set_complete writes result + status DONE; set_failed writes error + status FAILED.
   - Test get_jobs_by_status returns matching rows.

2. `tests/test_stub_graph.py`:
   - `test_stub_graph_completes_and_checkpoints` — per RESEARCH.md "Async Graph Test with Real AsyncSqliteSaver": ainvoke the compiled graph with a real AsyncSqliteSaver on a temp file; assert execution_result=="STUB: execution complete" and len(event_log)==3. Then REOPEN the DB with a fresh AsyncSqliteSaver + recompiled graph and assert `aget_state(config).values["execution_result"]` survives (proves PERSIST-01 across a DB-file reopen — the in-test analog of kill+restart).
   - EMPIRICAL TEST 1 (resume input None vs initial_state): After the first run completes on thread_id "resume-test", recompile a graph on the SAME db file + SAME thread_id and call `graph.astream(None, config, stream_mode="updates")`. Assert it does NOT re-run completed nodes (event_log length does not grow by 3). Then in a separate thread_id, document what passing the full initial_state does. Record the OBSERVED correct resume pattern in a top-of-file comment AND surface it in the SUMMARY (Phase 5 PERSIST-03 depends on this).
   - EMPIRICAL TEST 2 (astream chunk key format): In a fresh run, `async for chunk in graph.astream(initial, config, stream_mode="updates"): print(chunk)` and assert the FIRST chunk has exactly one key equal to "research" (the node name string, not a namespaced wrapper). Capture the literal observed key shape in a comment + SUMMARY.

If an empirical result contradicts RESEARCH.md's expectation, record the ACTUAL behavior — the goal is ground truth for the pinned langgraph==1.2.6, not confirmation.
  </action>
  <verify>
`uv run pytest tests/test_stub_graph.py tests/test_job_store.py -q -s` passes. The `-s` flag must show the printed raw astream chunk so the chunk-key shape is visible in output.
Manually confirm the SUMMARY records: (a) which resume input (None vs initial_state) skips completed nodes, (b) the literal astream chunk key shape.
  </verify>
  <done>All foundation tests pass; checkpoint survives a DB reopen; the resume-input question and the astream-chunk-key question are both empirically resolved and recorded for Phase 2/5 consumers.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q` — full suite green.
- `uv run python -c "import langgraph; assert langgraph.__version__ == '1.2.6'"` — version pinned.
- ORCH-04 guard: no `messages` field in OrchestratorState.
- PERSIST-01: checkpoint readable after AsyncSqliteSaver reopen on the same db file.
- PERSIST-02: jobs table exists with WAL mode; CRUD round-trips.
- Both empirical questions resolved and documented in the SUMMARY.
</verification>

<success_criteria>
- pyproject.toml pins langgraph==1.2.6, langgraph-checkpoint-sqlite==3.1.0, fastapi==0.137.2, uvicorn[standard]==0.49.0, aiosqlite, pydantic, python-dotenv.
- OrchestratorState exposes goal/job_id/research_findings/plan/execution_result/execution_status/event_log and NO messages accumulator (ORCH-04).
- build_stub_graph() yields research->plan->execute->END; compiles with AsyncSqliteSaver.
- JobStore + init_jobs_db provide WAL-mode jobs.db CRUD (PERSIST-02).
- Checkpoint survives DB reopen (PERSIST-01 proof).
- Empirical: resume-input pattern and astream-chunk-key format recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`. Explicitly record the two empirical findings (resume input None-vs-initial-state result; astream "updates" chunk key shape) — 01-02 and Phase 5 depend on these.
</output>
