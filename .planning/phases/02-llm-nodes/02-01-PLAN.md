---
phase: 02-llm-nodes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env
  - orchestrator/graph/llm.py
  - orchestrator/graph/state.py
  - orchestrator/graph/stub_graph.py
  - orchestrator/worker/runner.py
  - orchestrator/main.py
  - orchestrator/api/models.py
  - orchestrator/api/routes.py
  - tests/test_stub_graph.py
  - tests/test_worker.py
  - tests/test_api.py
autonomous: true

must_haves:
  truths:
    - "Submitting a goal makes research_node call qwen-122b via http://localhost:4000/v1 and writes non-empty research_findings into graph state"
    - "plan_node consumes goal + research_findings and writes a non-empty actionable plan into graph state via qwen-122b"
    - "The plan text (and research findings) for a DONE job is visible in the GET /jobs/{id}/result response"
    - "Each node records which model it used in state.node_models, and this survives a checkpoint reopen"
    - "All LLM calls route through LiteLLM :4000 via base_url from env (ORCH-05) — no mlx port hardcoded, and node bodies resolve the model from RESEARCH_MODEL/PLAN_MODEL env vars rather than a hardcoded model string"
    - "The existing Phase 1 tests still pass offline (stub-graph, worker, and API unit tests do not make real LLM calls)"
  artifacts:
    - path: "orchestrator/graph/llm.py"
      provides: "make_llm() factory reading LITELLM_BASE_URL/RESEARCH_MODEL/PLAN_MODEL from env"
      contains: "def make_llm"
    - path: "orchestrator/graph/state.py"
      provides: "OrchestratorState with node_models merge-reducer field (OBS-03)"
      contains: "node_models"
    - path: "orchestrator/graph/stub_graph.py"
      provides: "real research_node + plan_node (model from env), execute stub, build_stub_graph() + build_test_graph()"
      contains: "async def research_node"
    - path: "orchestrator/main.py"
      provides: "lifespan compiles build_test_graph() when ORCHESTRATOR_TEST_GRAPH=1, else build_stub_graph()"
      contains: "ORCHESTRATOR_TEST_GRAPH"
    - path: "orchestrator/api/models.py"
      provides: "JobResultResponse extended with plan and research_findings"
      contains: "plan"
    - path: "orchestrator/api/routes.py"
      provides: "GET /jobs/{id}/result reads plan/research_findings from checkpoint via app.state.graph.aget_state"
      contains: "aget_state"
    - path: "tests/test_worker.py"
      provides: "worker test migrated to build_test_graph() so it runs offline"
      contains: "build_test_graph"
    - path: "tests/test_api.py"
      provides: "API test sets ORCHESTRATOR_TEST_GRAPH=1 so the app graph is all-stub offline"
      contains: "ORCHESTRATOR_TEST_GRAPH"
    - path: "pyproject.toml"
      provides: "langchain-openai==1.3.2 pinned dependency"
      contains: "langchain-openai==1.3.2"
  key_links:
    - from: "orchestrator/graph/stub_graph.py:research_node"
      to: "orchestrator/graph/llm.py:make_llm"
      via: "make_llm(model=os.getenv('RESEARCH_MODEL','qwen-122b')).ainvoke(messages)"
      pattern: "make_llm\\(.*\\).*ainvoke|await llm.ainvoke"
    - from: "orchestrator/graph/stub_graph.py:research_node"
      to: "RESEARCH_MODEL/PLAN_MODEL env vars"
      via: "os.getenv('RESEARCH_MODEL'/'PLAN_MODEL', 'qwen-122b') resolves model (ORCH-05, no hardcoded model literal)"
      pattern: "RESEARCH_MODEL|PLAN_MODEL"
    - from: "orchestrator/graph/llm.py:make_llm"
      to: "LiteLLM :4000"
      via: "ChatOpenAI(base_url=os.getenv('LITELLM_BASE_URL'))"
      pattern: "base_url.*LITELLM_BASE_URL|getenv\\(.LITELLM_BASE_URL"
    - from: "orchestrator/main.py:lifespan"
      to: "build_test_graph / build_stub_graph"
      via: "ORCHESTRATOR_TEST_GRAPH env flag selects the compiled graph"
      pattern: "ORCHESTRATOR_TEST_GRAPH"
    - from: "orchestrator/api/routes.py:get_result"
      to: "app.state.graph checkpoint"
      via: "await request.app.state.graph.aget_state(config)"
      pattern: "aget_state"
    - from: "orchestrator/worker/runner.py:initial_state"
      to: "OrchestratorState.node_models"
      via: "initial_state['node_models'] = {}"
      pattern: "node_models.*\\{\\}"
---

<objective>
Replace the Phase 1 research/plan stub nodes with REAL nodes that call qwen-122b
through the LiteLLM proxy at :4000, add OBS-03 per-node model attribution to graph
state, and expose the generated plan via the result API. Execute stays a stub
(Phase 3 replaces it).

Purpose: This is the core of Phase 2 — it makes the orchestrator produce real LLM
research and plans (ORCH-02, ORCH-03), routes every call through LiteLLM (ORCH-05),
and records model attribution per node (OBS-03), while keeping ORCH-04 (no unbounded
accumulator) intact.

Output: orchestrator/graph/llm.py (new factory), modified state.py / stub_graph.py /
runner.py / main.py / api models + routes, pinned langchain-openai dependency, and
updated unit tests (stub-graph, worker, API) that keep Phase 1 green offline without
making network calls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-llm-nodes/02-RESEARCH.md

# Files this plan modifies — read before editing
@orchestrator/graph/state.py
@orchestrator/graph/stub_graph.py
@orchestrator/worker/runner.py
@orchestrator/main.py
@orchestrator/api/models.py
@orchestrator/api/routes.py
@orchestrator/persistence/job_store.py
@tests/test_stub_graph.py
@tests/test_worker.py
@tests/test_api.py
</context>

<critical_constraints>
- ORCH-05: base_url AND model names MUST come from env (LITELLM_BASE_URL, RESEARCH_MODEL,
  PLAN_MODEL). NEVER hardcode "http://localhost:4000" in node bodies, and NEVER bake a
  hardcoded "qwen-122b" literal into the node body as the model — resolve the model via
  `os.getenv("RESEARCH_MODEL", "qwen-122b")` / `os.getenv("PLAN_MODEL", "qwen-122b")` (the
  "qwen-122b" string may appear ONLY as the getenv default, never as a bare assignment).
  This makes ORCH-05 grep-verifiable. api_key is the literal string "dummy".
- max_retries=0 on every ChatOpenAI instance (RESEARCH Anti-Pattern): retries amplify load
  on a wedged 122B. Let the job FAIL instead.
- streaming=False (RESEARCH key finding): LiteLLM 1.86.1 has NO 504 issue; non-streaming
  131s call succeeded. ainvoke() returns a complete AIMessage regardless.
- max_tokens bounded: research=2000, plan=1500 (RESEARCH Pitfall 5 — keep 122B from wedging).
  timeout=300.
- NEVER instantiate ChatOpenAI at module top-level (RESEARCH Pitfall 2): env vars are read
  before load_dotenv() runs. make_llm() is called inside node functions only.
- node_models MUST be initialized as {} (empty dict), never None (RESEARCH Pitfall 3 — the
  merge reducer does {**left, **right} and crashes on None).
- ORCH-04: node_models is bounded (max 3 entries, one per node). It is NOT an unbounded
  accumulator. Do not add any list-of-messages field.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Pin langchain-openai + add LiteLLM env config + make_llm() factory</name>
  <files>pyproject.toml, .env, orchestrator/graph/llm.py</files>
  <action>
  1. pyproject.toml: add `"langchain-openai==1.3.2",` to the [project] dependencies list
     (it is already installed in .venv but unlisted). Keep all existing pins unchanged.
  2. .env: append three lines (RESEARCH_MODEL/PLAN_MODEL are the env source ORCH-05 + the
     node bodies read — they are NOT optional):
       LITELLM_BASE_URL=http://localhost:4000/v1
       RESEARCH_MODEL=qwen-122b
       PLAN_MODEL=qwen-122b
  3. Create orchestrator/graph/llm.py with a single make_llm() factory exactly as
     RESEARCH.md Pattern 1 specifies:
       import os
       from langchain_openai import ChatOpenAI
       def make_llm(model=None, max_tokens=2000, temperature=0.3, timeout=300) -> ChatOpenAI:
           base_url = os.getenv("LITELLM_BASE_URL", "http://localhost:4000/v1")
           resolved_model = model or os.getenv("RESEARCH_MODEL", "qwen-122b")
           return ChatOpenAI(model=resolved_model, base_url=base_url, api_key="dummy",
                             streaming=False, max_tokens=max_tokens, temperature=temperature,
                             timeout=timeout, max_retries=0)
     Add a module docstring noting: env-based config (ORCH-05), never a singleton, never
     instantiated at import time, max_retries=0 to avoid amplifying load on a wedged 122B.
  </action>
  <verify>
  - `.venv/bin/python -c "from orchestrator.graph.llm import make_llm; llm = make_llm(); print(type(llm).__name__, llm.model_name)"` prints `ChatOpenAI qwen-122b`.
  - `grep -q 'langchain-openai==1.3.2' pyproject.toml` succeeds.
  - `grep -q 'LITELLM_BASE_URL=http://localhost:4000/v1' .env` succeeds.
  - `grep -q 'RESEARCH_MODEL=qwen-122b' .env && grep -q 'PLAN_MODEL=qwen-122b' .env` succeeds.
  - `grep -n 'max_retries=0' orchestrator/graph/llm.py` matches.
  </verify>
  <done>make_llm() returns a ChatOpenAI pointed at the env base_url with model qwen-122b, streaming=False, max_retries=0, timeout=300; langchain-openai==1.3.2 is in pyproject; LITELLM_BASE_URL, RESEARCH_MODEL, and PLAN_MODEL are in .env.</done>
</task>

<task type="auto">
  <name>Task 2: Add OBS-03 node_models to state; make research_node + plan_node real (model from env); keep execute stub; split test graph builder; migrate worker test offline</name>
  <files>orchestrator/graph/state.py, orchestrator/graph/stub_graph.py, orchestrator/worker/runner.py, tests/test_stub_graph.py, tests/test_worker.py</files>
  <action>
  Execute these sub-steps IN ORDER. Each is independently verifiable so a mid-task failure is
  locatable.

  2a. STATE + RUNNER (reducer + seed):
     - state.py: add the node_models field + _merge_dicts reducer exactly as RESEARCH.md
       Pattern 4 / "Updated state.py" example:
         def _merge_dicts(left: dict, right: dict) -> dict:
             return {**left, **right}
       and on OrchestratorState:
         node_models: Annotated[dict, _merge_dicts]
       Comment it as OBS-03, bounded (3 entries max), ORCH-04 compliant.
     - runner.py: in initial_state dict add `"node_models": {},` (RESEARCH Pitfall 3 — empty
       dict, NOT None). Do not change any other worker logic — astream composition is already
       correct (RESEARCH key finding: real async nodes compose with the existing
       graph.astream(stream_mode="updates"); no worker change needed beyond initial_state).

  2b. REAL NODES + DUAL BUILDERS (stub_graph.py), per RESEARCH.md "Complete research_node +
      plan_node" code block, BUT with the model resolved from env (ORCH-05, B3 fix):
     - Replace research_stub -> async def research_node(state): builds messages with
       _RESEARCH_SYSTEM prompt, resolves the model from env (NOT a hardcoded literal):
         model = os.getenv("RESEARCH_MODEL", "qwen-122b")
         llm = make_llm(model=model, max_tokens=2000, temperature=0.4)
       calls await llm.ainvoke([...]), returns
       {"research_findings": response.content, "node_models": {"research": model},
        "event_log": [f"{utc_iso} research_node: complete model={model}"]}.
       Wrap the ainvoke in try/except (APIConnectionError, APITimeoutError, APIStatusError)
       re-raising as RuntimeError (RESEARCH Pattern 5) so the worker set_failed() path catches
       it. Import these from `openai`. Do NOT hardcode base_url here (make_llm resolves it from
       env) and do NOT bake the model literal into the body — the only "qwen-122b" allowed is
       the os.getenv default. This satisfies ORCH-05 and is grep-verifiable.
     - Replace plan_stub -> async def plan_node(state): _PLAN_SYSTEM prompt, includes
       goal + state["research_findings"]; resolve model from env:
         model = os.getenv("PLAN_MODEL", "qwen-122b")
         llm = make_llm(model=model, max_tokens=1500, temperature=0.2)
       Same try/except. Returns {"plan": response.content,
       "node_models": {"plan": model}, "event_log": [...]}.
     - Keep execute_stub UNCHANGED (async sleep 2.0, returns execution_result/status, still
       returns "STUB: execution complete").
     - Keep build_stub_graph() name and node string keys "research"/"plan"/"execute"
       (the worker maps these to status transitions — do NOT rename).
     - ADD a second builder build_test_graph() that wires the OLD all-stub nodes
       (research_stub, plan_stub, execute_stub) so unit/worker tests never hit the network.
       Keep research_stub and plan_stub functions in the file for this. build_test_graph()
       has identical graph shape but uses the three *_stub functions.

  2c. MIGRATE OFFLINE UNIT TESTS (stub-graph + worker):
     - tests/test_stub_graph.py: these are Phase 1 UNIT tests that must stay offline.
       Point all three tests at build_test_graph() instead of build_stub_graph()
       (so research/plan use stubs, no network). Update the import accordingly.
       Add `"node_models": {}` to the _make_initial_state() helper dict so the merge
       reducer has a dict to start from. Keep all existing assertions (event_log len == 3,
       execution_result == "STUB: execution complete", chunk keys
       ["research","plan","execute"]) — they remain valid for the stub graph.
     - tests/test_worker.py: this imports build_stub_graph and asserts
       result == "STUB: execution complete". After this task build_stub_graph() makes REAL
       qwen-122b calls, so the worker test would hit the network / fail offline. Migrate it to
       import and compile build_test_graph() (all-stub) instead of build_stub_graph().
       KEEP the existing `result == "STUB: execution complete"` assertion — the stub execute
       node still returns that string under build_test_graph(). Seed `"node_models": {}` in any
       initial_state the worker harness builds so the reducer has a dict to start from. Do NOT
       weaken or remove the existing status-transition / result assertions.
  </action>
  <verify>
  - `.venv/bin/python -c "from orchestrator.graph.stub_graph import build_stub_graph, build_test_graph, research_node, plan_node; print('ok')"` prints ok.
  - `.venv/bin/python -c "import inspect; from orchestrator.graph import stub_graph as s; src=inspect.getsource(s); assert 'make_llm' in src and 'node_models' in src and 'execute_stub' in src; print('shapes ok')"` prints shapes ok.
  - ORCH-05 grep (B3/W7): `grep -n 'RESEARCH_MODEL\|PLAN_MODEL' orchestrator/graph/stub_graph.py` matches (proves nodes read the model from env, not a hardcoded literal).
  - `grep -n 'node_models' orchestrator/graph/state.py orchestrator/worker/runner.py | grep -q node_models` succeeds.
  - `grep -n 'build_test_graph' tests/test_stub_graph.py tests/test_worker.py` matches in BOTH files (worker test migrated offline).
  - `.venv/bin/python -m pytest tests/test_stub_graph.py tests/test_worker.py -q` passes OFFLINE (no LiteLLM running): both use build_test_graph(), worker test still asserts result == "STUB: execution complete".
  </verify>
  <done>state.py has node_models merge field; runner initial_state seeds node_models={}; research_node and plan_node resolve their model via os.getenv(RESEARCH_MODEL/PLAN_MODEL) and call make_llm().ainvoke() with bounded max_tokens, re-raising API errors as RuntimeError; no bare hardcoded "qwen-122b" model assignment in node bodies (grep for RESEARCH_MODEL/PLAN_MODEL matches); execute_stub unchanged; build_stub_graph() is the real graph and build_test_graph() is the all-stub graph; test_stub_graph.py AND test_worker.py both use build_test_graph() and pass offline with the "STUB: execution complete" assertion intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add ORCHESTRATOR_TEST_GRAPH flag to lifespan; expose plan + research_findings in result API; migrate API test offline (criterion 2 visibility)</name>
  <files>orchestrator/main.py, orchestrator/api/models.py, orchestrator/api/routes.py, tests/test_api.py</files>
  <action>
  Resolve RESEARCH Open Question 3 with the simplest option: enrich the existing
  GET /jobs/{id}/result response with the plan text read from the checkpoint, and make the
  app graph offline-selectable so test_api.py does not require LiteLLM. Keep API-03 behavior
  (404 unknown, 409 until DONE) intact.

  1. main.py lifespan (offline-graph flag — B2 fix, this is a REQUIRED step, not a note):
     - In the lifespan/startup that currently compiles the graph, read an env flag and pick
       the builder:
         import os
         from orchestrator.graph.stub_graph import build_stub_graph, build_test_graph
         if os.getenv("ORCHESTRATOR_TEST_GRAPH") == "1":
             graph_builder = build_test_graph   # all-stub, no network (tests)
         else:
             graph_builder = build_stub_graph    # real qwen-122b nodes (production)
       Compile that builder with the AsyncSqliteSaver exactly as today and assign to
       app.state.graph. Do not change saver.setup()/init_jobs_db ordering.

  2. api/models.py: extend JobResultResponse with two optional fields:
       plan: str | None = None
       research_findings: str | None = None
     (result stays as-is — it carries execution_result from jobs.db.)

  3. api/routes.py get_result handler:
     - Add `request: Request` param so the handler can reach app.state.graph
       (import Request is already present).
     - Keep the existing 404 (unknown) and 409 (status != DONE) guards FIRST, unchanged.
     - After the DONE guard, read the checkpoint state for this job:
         config = {"configurable": {"thread_id": job_id}}
         snapshot = await request.app.state.graph.aget_state(config)
         values = snapshot.values if snapshot else {}
         plan = values.get("plan")
         research = values.get("research_findings")
     - Return JobResultResponse(job_id=job_id, result=job["result"], plan=plan,
       research_findings=research).
     - Wrap the aget_state call defensively: if it raises, fall back to plan=None,
       research_findings=None (the result field from jobs.db is the contract; the
       checkpoint enrichment is best-effort) — but do NOT swallow the 404/409 logic.

  4. tests/test_api.py (offline-safe — B2 fix, REQUIRED step):
     - In the test client fixture/helper (_make_client() or equivalent), set the env flag
       BEFORE the app/lifespan starts so the app compiles the all-stub graph and never calls
       LiteLLM: `monkeypatch.setenv("ORCHESTRATOR_TEST_GRAPH", "1")` (or os.environ set before
       constructing the TestClient/ASGITransport, whichever the existing harness uses).
     - Keep the existing assertion that result == "STUB: execution complete" (the all-stub
       execute node still returns it). plan/research_findings will be None for the stub run
       (stub nodes do not write them) — that is expected and acceptable; do not assert they are
       non-empty here (the real-data plan visibility is proven live in 02-02).
     - Do NOT weaken the existing 404/409 (API-03) assertions.
  </action>
  <verify>
  - `.venv/bin/python -c "from orchestrator.api.models import JobResultResponse; m=JobResultResponse(job_id='x', result='r'); assert m.plan is None and m.research_findings is None; print('model ok')"` prints model ok.
  - `grep -n 'aget_state' orchestrator/api/routes.py` matches.
  - `grep -n 'ORCHESTRATOR_TEST_GRAPH' orchestrator/main.py` matches; `grep -n 'ORCHESTRATOR_TEST_GRAPH' tests/test_api.py` matches.
  - `.venv/bin/python -m pytest tests/test_api.py -q` passes OFFLINE (no LiteLLM running): the app compiles build_test_graph via ORCHESTRATOR_TEST_GRAPH=1, so research/plan nodes are stubs and the job reaches DONE with result == "STUB: execution complete"; 404/409 guards still pass.
  - Full suite offline: `.venv/bin/python -m pytest -q` → all tests pass (no LiteLLM required; nothing hangs).
  </verify>
  <done>main.py lifespan selects build_test_graph() when ORCHESTRATOR_TEST_GRAPH=1 else build_stub_graph(); GET /jobs/{id}/result returns plan + research_findings from the checkpoint for DONE jobs while preserving 404/409 behavior; JobResultResponse carries plan + research_findings; test_api.py sets ORCHESTRATOR_TEST_GRAPH=1 and passes offline; the full test suite passes offline without LiteLLM.</done>
</task>

</tasks>

<verification>
- make_llm() reads base_url + model from env (ORCH-05); no hardcoded :4000 in node bodies, and node bodies resolve the model via os.getenv(RESEARCH_MODEL/PLAN_MODEL) — `grep -n 'RESEARCH_MODEL\|PLAN_MODEL' orchestrator/graph/stub_graph.py` matches (W7).
- research_node and plan_node call qwen-122b via make_llm().ainvoke() with bounded max_tokens and re-raise API errors as RuntimeError.
- node_models merge field added to state; initial_state seeds {} (OBS-03, ORCH-04 bounded).
- execute_stub unchanged (Phase 3 owns it).
- GET /jobs/{id}/result exposes plan + research_findings from the checkpoint (criterion 2).
- build_test_graph() + ORCHESTRATOR_TEST_GRAPH flag keep ALL unit/worker/API tests offline; full suite passes with NO LiteLLM running: `.venv/bin/python -m pytest -q`.
</verification>

<success_criteria>
- Maps to ORCH-02, ORCH-03, ORCH-05, OBS-03 (partial — full end-to-end proof lives in 02-02).
- Phase success criteria 1 and 2 are implemented here (real research/plan + plan visible in result API).
- All Phase 1 tests (stub-graph, worker, API) remain green offline with no LiteLLM running.
</success_criteria>

<output>
After completion, create `.planning/phases/02-llm-nodes/02-01-SUMMARY.md` documenting:
- The make_llm() env contract and why max_retries=0 / streaming=False.
- That node bodies resolve the model from RESEARCH_MODEL/PLAN_MODEL env (ORCH-05), with no
  hardcoded model literal in the node body.
- The node_models OBS-03 schema and how it survives checkpoint.
- The build_stub_graph (real) vs build_test_graph (offline) split and the
  ORCHESTRATOR_TEST_GRAPH env flag in main.py lifespan — 02-02 and Phase 3 must know this
  exists, and that test_worker.py + test_api.py both rely on the all-stub path to run offline.
- The result API enrichment (plan/research_findings from aget_state).
</output>
