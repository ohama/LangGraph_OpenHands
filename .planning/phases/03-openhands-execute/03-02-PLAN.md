---
phase: 03-openhands-execute
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - orchestrator/graph/stub_graph.py
  - tests/test_execute_node.py
autonomous: true

must_haves:
  truths:
    - "build_stub_graph() uses the real execute_node (not execute_stub); build_test_graph() still uses execute_stub so all offline tests stay SDK-free"
    - "execute_node wraps the blocking run_openhands() via asyncio.to_thread under asyncio.wait_for(timeout) so the event loop is never blocked (EXEC-02)"
    - "execute_node computes a per-job workspace under EXECUTE_WORKSPACE_BASE/<job_id>/workspace/ and asserts it is NOT the orchestrator cwd (EXEC-03)"
    - "research.md and plan.md are written to the workspace BEFORE the agent runs; execution_summary.md after — so artifacts exist even on failure (OBS-02)"
    - "execute_node records node_models={'execute': <model>} and returns execution_result/execution_status (OBS-03)"
    - "openhands.* is imported LAZILY inside execute_node body (via the adapter) so importing stub_graph never loads openhands (EXEC-05)"
    - "execute_node is unit-testable with run_openhands monkeypatched to a stub — no openhands.* import in the test path"
  artifacts:
    - path: "orchestrator/graph/stub_graph.py"
      provides: "real execute_node in build_stub_graph(); execute_stub retained for build_test_graph()"
      contains: "async def execute_node"
    - path: "tests/test_execute_node.py"
      provides: "offline unit tests: workspace isolation, artifact writes, status mapping, to_thread non-block, lazy-import isolation"
      contains: "monkeypatch"
  key_links:
    - from: "orchestrator/graph/stub_graph.py execute_node"
      to: "orchestrator.execution.openhands_adapter.run_openhands"
      via: "lazy import inside function body, called via asyncio.to_thread under asyncio.wait_for"
      pattern: "asyncio.to_thread"
    - from: "execute_node"
      to: "~/projs/langgraph-jobs/<job_id>/workspace/"
      via: "Path(workspace_base)/job_id/'workspace', mkdir + assert != cwd"
      pattern: "EXECUTE_WORKSPACE_BASE"
    - from: "execute_node return"
      to: "OrchestratorState.execution_result/execution_status/node_models"
      via: "returned partial-state dict merged by LangGraph"
      pattern: "node_models.*execute"
---

<objective>
Replace execute_stub with a real async execute_node in build_stub_graph() that drives the OpenHands adapter off-thread, isolates each job's workspace, writes artifacts, enforces a wall-clock timeout, and maps SDK results to job status — while keeping build_test_graph() all-stub so the existing 20-test offline suite stays green and SDK-free.

Purpose: This is the wiring that makes Research→Plan→Execute complete autonomously (ORCH-01) with the event loop responsive during a multi-minute run (EXEC-02), per-job isolation (EXEC-03), the iterations cap honored (EXEC-04), and artifacts persisted (OBS-02). The lazy import is what preserves EXEC-05 at the graph layer: stub_graph.py must NOT load openhands.* at import time.
Output: a real execute_node in stub_graph.py + offline unit tests proving isolation, artifacts, timeout, and status mapping with run_openhands stubbed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-openhands-execute/03-RESEARCH.md

# 03-01 built the adapter this node calls. execute_node body is spelled out in
# RESEARCH Pattern 3 (the async wrapper) — use it as the source of truth.
@orchestrator/graph/stub_graph.py
@orchestrator/graph/state.py
@orchestrator/worker/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real execute_node to stub_graph.py; wire into build_stub_graph(); keep execute_stub for build_test_graph()</name>
  <files>orchestrator/graph/stub_graph.py</files>
  <action>
    Implement execute_node per RESEARCH Pattern 3, then swap it into build_stub_graph() ONLY. Do NOT remove execute_stub — build_test_graph() must keep using it (offline tests + EXEC-05).

    1. Add async def execute_node(state: OrchestratorState) -> dict at module scope. Keep ALL openhands-touching imports lazy INSIDE the function body (EXEC-05 / RESEARCH Pitfall 1):
         from orchestrator.execution.openhands_adapter import run_openhands, serialize_transcript
       Also do `import asyncio` at module top is fine (already imported), but the adapter import MUST be inside the function so importing stub_graph never pulls openhands.

    2. Inside execute_node:
       - model = os.getenv("EXECUTE_MODEL", "openai/qwen-35b")
       - max_iterations = int(os.getenv("EXECUTE_MAX_ITERATIONS", "20"))
       - timeout_secs = float(os.getenv("EXECUTE_TIMEOUT_SECS", "900"))
       - workspace_base = os.getenv("EXECUTE_WORKSPACE_BASE", str(Path.home()/"projs"/"langgraph-jobs"))
       - job_id = state["job_id"]; job_workspace = str(Path(workspace_base)/job_id/"workspace"); mkdir(parents=True, exist_ok=True)
       - EXEC-03 SAFETY ASSERT (RESEARCH Pitfall 5): assert Path(job_workspace).resolve() != Path.cwd().resolve(), to guarantee the agent never runs in the orchestrator source tree. (Use an assertion or an explicit raise RuntimeError — must hard-fail if workspace == cwd.)

    3. OBS-02 pre-run artifacts (write BEFORE running so they survive failure):
       - _write_artifact(job_workspace, "research.md", state.get("research_findings") or "")
       - _write_artifact(job_workspace, "plan.md", state.get("plan") or "")

    4. EXEC-02 off-thread run with wall-clock timeout:
         result = await asyncio.wait_for(
             asyncio.to_thread(run_openhands, goal=state["goal"], plan=state.get("plan") or "",
                               workspace_dir=job_workspace, max_iterations=max_iterations, timeout_secs=timeout_secs),
             timeout=timeout_secs)
       - except asyncio.TimeoutError: result = {execution_result: f"Execution timed out after {timeout_secs}s", execution_status:"failed", iterations_used:0, final_sdk_status:"timeout", event_count:0}. (RESEARCH Pitfall 2: the orphaned thread is NOT killable, but the per-call LLM timeout=300 bounds it and the event loop stays responsive — that is acceptable and is the documented behavior.)
       - except Exception as exc: result = {... execution_status:"failed", final_sdk_status:"error" ...}.

    5. OBS-02 post-run: write execution_summary.md (job_id, model, started_at/completed_at ISO-8601 UTC, status, sdk_status, iterations_used, event_count, and a "## Result" section with execution_result) per RESEARCH Pattern 3. (execution_transcript.jsonl is written by run_openhands itself in 03-01; do not duplicate it here.)

    6. Return partial state:
         {"execution_result": result.get("execution_result",""),
          "execution_status": result.get("execution_status","failed"),
          "node_models": {"execute": model},            # OBS-03 — merges via _merge_dicts
          "event_log": [f"{completed_at} execute_node: complete status=... iterations=... sdk_status=..."]}
       The "execution_status" values MUST be one of success|partial|failed (matches state.py docstring + worker DONE handling). Note: the worker maps the "execute" node COMPLETING to job status DONE regardless of execution_status (see runner.py _NODE_COMPLETE_TO_STATUS); a "partial"/"failed" execution_status still ends the job at DONE with the result/status recorded in state — this is intended (criterion 4: cap → DONE). Do NOT raise from execute_node on partial/timeout (raising would flip the job to FAILED via the worker except-block; only a truly unexpected error should propagate). Confirm this matches the success criteria: cap→DONE-or-FAILED is satisfied because the node returns normally with execution_status="partial".

    7. Add module-scope helper _write_artifact(workspace_dir, filename, content) -> None: Path(workspace_dir)/filename.write_text(content, encoding="utf-8") wrapped in try/except pass (RESEARCH Pattern 3).

    8. In build_stub_graph(): change `builder.add_node("execute", execute_stub)` → `builder.add_node("execute", execute_node)`. Node string key stays "execute" (runner.py _NODE_COMPLETE_TO_STATUS depends on it — do NOT rename). Leave build_test_graph() untouched (still execute_stub). Update the module docstring node-split comment to reflect execute is now REAL in build_stub_graph().

    Add `from pathlib import Path` to the module imports if not present.
  </action>
  <verify>
    `grep -q "async def execute_node" orchestrator/graph/stub_graph.py` succeeds.
    `grep -A40 "def build_stub_graph" orchestrator/graph/stub_graph.py | grep -q 'add_node("execute", execute_node)'` succeeds.
    `grep -A40 "def build_test_graph" orchestrator/graph/stub_graph.py | grep -q 'add_node("execute", execute_stub)'` succeeds (test graph unchanged).
    EXEC-05 stays clean: `grep -E "from openhands|import openhands" orchestrator/graph/stub_graph.py` returns NOTHING (the adapter import is inside the function body, lazy).
    Importing stub_graph does NOT load openhands: `.venv/bin/python -c "import sys; import orchestrator.graph.stub_graph; assert 'openhands' not in sys.modules, 'openhands loaded at import!'; print('NO-OPENHANDS-AT-IMPORT')"` prints the success line.
    `grep -q "asyncio.to_thread" orchestrator/graph/stub_graph.py` and `grep -q "asyncio.wait_for" orchestrator/graph/stub_graph.py` both succeed (EXEC-02).
    `grep -q "EXECUTE_WORKSPACE_BASE" orchestrator/graph/stub_graph.py` succeeds (EXEC-03).
  </verify>
  <done>
    build_stub_graph() routes "execute" to the real execute_node; build_test_graph() still routes to execute_stub. execute_node lazily imports the adapter, runs it via asyncio.to_thread under asyncio.wait_for(timeout), computes+asserts a per-job workspace outside cwd, writes research.md/plan.md before and execution_summary.md after, maps cap/timeout to a non-raising partial/failed result, and records node_models["execute"]. Importing stub_graph loads zero openhands modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Offline unit tests for execute_node with run_openhands stubbed (no openhands import)</name>
  <files>tests/test_execute_node.py</files>
  <action>
    Write pytest tests (asyncio_mode=auto is set in pyproject) that exercise execute_node WITHOUT importing openhands — by monkeypatching the adapter's run_openhands. This proves EXEC-05 stubbability and EXEC-03/OBS-02/EXEC-02 behavior offline.

    Use tmp_path + monkeypatch.setenv("EXECUTE_WORKSPACE_BASE", str(tmp_path)) so workspaces land in a temp dir.

    Patch strategy: monkeypatch the symbol the node imports. Because execute_node does `from orchestrator.execution.openhands_adapter import run_openhands` lazily, patch `orchestrator.execution.openhands_adapter.run_openhands` (and serialize_transcript) with a fake that does NOT import openhands. The fake run_openhands should accept (goal, plan, workspace_dir, max_iterations, timeout_secs), optionally write a sentinel file into workspace_dir, and return a controllable result dict. (To make the lazy import resolve without openhands installed, you may need to inject a stub module into sys.modules for orchestrator.execution.openhands_adapter, OR — simpler — monkeypatch.setattr on the real module's run_openhands. The real adapter module imports openhands at module top; if that import is heavy/undesired in unit tests, register a fake module: sys.modules["orchestrator.execution.openhands_adapter"] = types.SimpleNamespace(run_openhands=fake, serialize_transcript=lambda *a, **k: None) BEFORE calling execute_node. Document whichever approach is used.)

    Tests:
    1. test_execute_node_writes_artifacts_and_status:
       fake run_openhands returns {"execution_result":"done","execution_status":"success","iterations_used":3,"final_sdk_status":"finished","event_count":5}. Call await execute_node({"goal":"g","job_id":"job-test-1","research_findings":"R","plan":"P","execution_result":None,"execution_status":None,"event_log":[],"node_models":{}}). Assert returned dict has execution_status=="success", node_models=={"execute": <model>}, execution_result=="done". Assert tmp_path/"job-test-1"/"workspace"/"research.md" contains "R", plan.md contains "P", execution_summary.md exists.
    2. test_execute_node_partial_on_cap:
       fake returns execution_status="partial", final_sdk_status="error". Assert execute_node returns execution_status=="partial" and does NOT raise (criterion 4: cap → terminal, node returns normally). Assert execution_summary.md records sdk_status error/partial.
    3. test_execute_node_timeout_does_not_block:
       fake run_openhands sleeps (time.sleep) longer than a tiny EXECUTE_TIMEOUT_SECS (monkeypatch.setenv("EXECUTE_TIMEOUT_SECS","0.2")). Wrap the call: while execute_node awaits, schedule a concurrent coroutine that records loop responsiveness (e.g., asyncio.gather(execute_node(...), a probe that sets a flag after asyncio.sleep(0.05))). Assert the probe completed BEFORE execute_node returned (proves to_thread did not block the loop) AND execute_node returned execution_status=="failed" with final_sdk_status=="timeout". (This is the offline analogue of criterion 2; the live proof is in 03-03.)
    4. test_execute_node_workspace_not_cwd:
       Assert that with EXECUTE_WORKSPACE_BASE=tmp_path, the workspace path resolved by the node is under tmp_path and != Path.cwd() (EXEC-03). If feasible, also assert the node raises/asserts when forced workspace==cwd (e.g., monkeypatch EXECUTE_WORKSPACE_BASE to "." and a job_id of "" — expect AssertionError/RuntimeError).
    5. test_stub_graph_imports_without_openhands:
       import orchestrator.graph.stub_graph; assert "openhands" not in sys.modules (EXEC-05 at import time).

    Mark NONE of these skipif — they must run in the default offline suite (no :4000, no openhands needed in the import path of the test).
  </action>
  <verify>
    Offline suite green INCLUDING new tests: `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest tests/test_execute_node.py -q` passes with 0 failures.
    Full offline suite still green: `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest -q` passes (previous 20 tests + new ones), and produces no openhands banner / litellm warning in output (proves no openhands import in the offline path).
    `grep -q "monkeypatch" tests/test_execute_node.py` and `grep -q "to_thread\|responsiveness\|probe\|gather" tests/test_execute_node.py` succeed.
  </verify>
  <done>
    tests/test_execute_node.py runs fully offline (no openhands, no :4000), proving: artifacts written + status mapping, partial-on-cap without raising, timeout→failed while the event loop stayed responsive, workspace isolated from cwd, and stub_graph imports without loading openhands. Full offline suite is green and SDK/network-free.
  </done>
</task>

</tasks>

<verification>
- build_stub_graph() uses execute_node; build_test_graph() unchanged (execute_stub) — Phase 1/2 offline tests untouched.
- EXEC-05: `grep -rE "from openhands|import openhands" orchestrator/graph orchestrator/worker orchestrator/main.py orchestrator/api` is empty; importing stub_graph loads zero openhands modules; execute_node stubbable via run_openhands monkeypatch.
- EXEC-02: asyncio.to_thread under asyncio.wait_for; offline test proves loop stays responsive during the (faked) blocking run.
- EXEC-03: per-job workspace under EXECUTE_WORKSPACE_BASE/<job_id>/workspace with a hard assert that it is not cwd.
- EXEC-04: cap/timeout map to non-raising partial/failed → job ends at DONE (worker maps execute-complete→DONE) rather than running forever.
- OBS-02/OBS-03: research.md/plan.md/execution_summary.md written; node_models["execute"] recorded.
- `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest -q` fully green.
</verification>

<success_criteria>
- The pipeline's execute node is real and event-loop-safe; the offline test suite proves isolation, artifacts, timeout-non-block, and status mapping without any live stack.
- EXEC-05 holds at the graph layer (lazy import); the 20 prior tests stay green and SDK-free.
- A would-be-runaway execute returns a terminal partial/failed result instead of raising or hanging.
</success_criteria>

<output>
After completion, create `.planning/phases/03-openhands-execute/03-02-SUMMARY.md`.
Record: the monkeypatch/stub-module approach chosen for testing the lazy adapter import, the offline test count (prior 20 + new), and confirmation that importing stub_graph loads zero openhands modules.
</output>
