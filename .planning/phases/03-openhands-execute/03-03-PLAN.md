---
phase: 03-openhands-execute
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/verify_phase3.sh
  - scripts/cap_proof.py
  - tests/test_execute_integration.py
autonomous: false

must_haves:
  truths:
    - "A submitted goal runs Research→Plan→Execute to DONE with no approval gate; status transitions PENDING→RESEARCHING→PLANNING→EXECUTING→DONE automatically (criterion 1, ORCH-01)"
    - "While the job is EXECUTING, repeated GET /jobs/{id}/status calls return with sub-second latency — the event loop is NOT blocked (criterion 2, EXEC-02)"
    - "Per-job artifacts research.md, plan.md, execution_transcript.jsonl exist under ~/projs/langgraph-jobs/<job_id>/workspace/ (criterion 3, OBS-02)"
    - "The orchestrator source tree is byte-for-byte unmodified by the run (criterion 3 — agent never runs in orchestrator cwd)"
    - "An execute run forced toward the iterations cap terminates at the cap and ends DONE-or-FAILED, never runs forever (criterion 4, EXEC-04)"
    - "node_models records execute's model openai/qwen-35b alongside research/plan, and verify_phase3.sh asserts it via GET /jobs/{id}/result (OBS-03 consistency)"
  artifacts:
    - path: "scripts/verify_phase3.sh"
      provides: "end-to-end live proof: launch .venv/bin/uvicorn (no orphan), submit goal, assert PLANNING→EXECUTING→DONE, sub-second status during EXECUTING, workspace artifacts, source-tree-untouched, node_models.execute assertion"
      contains: ".venv/bin/uvicorn"
    - path: "scripts/cap_proof.py"
      provides: "max-iterations cap proof — run with a tiny EXECUTE_MAX_ITERATIONS against a looping goal, assert terminal status at cap"
      contains: "EXECUTE_MAX_ITERATIONS"
    - path: "tests/test_execute_integration.py"
      provides: "skippable-when-:4000-down integration tests (full pipeline to DONE; offline stub pipeline)"
      contains: "skipif"
  key_links:
    - from: "scripts/verify_phase3.sh"
      to: "running orchestrator service on a non-colliding port (8099)"
      via: ".venv/bin/uvicorn orchestrator.main:app --workers 1 --port 8099, EXIT-trap pkill, lsof port-free wait"
      pattern: "8099"
    - from: "verify_phase3.sh status poll"
      to: "GET /jobs/{id}/status latency assertion during EXECUTING"
      via: "curl -w %{time_total} repeatedly while status==EXECUTING, assert < 1.0s"
      pattern: "time_total"
    - from: "verify_phase3.sh"
      to: "checksum of orchestrator source"
      via: "shasum manifest before run, diff after run — assert no source files changed (git diff --stat orchestrator/ is the conceptual rationale; shasum is the portable check)"
      pattern: "shasum"
    - from: "verify_phase3.sh"
      to: "GET /jobs/{id}/result node_models.execute"
      via: "curl .../result | jq -e '.node_models.execute == \"openai/qwen-35b\"'"
      pattern: "node_models.execute"
---

<objective>
Prove the full Phase 3 contract against the LIVE stack: a real goal runs Research→Plan→Execute to DONE autonomously, the status endpoint stays sub-second responsive while OpenHands executes, per-job artifacts land in the isolated workspace, the orchestrator source is never touched, a runaway execute terminates at the iterations cap, and node_models records execute=openai/qwen-35b. Because these proofs are slow (multi-minute 35B runs) and require :4000+qwen-35b, this plan ends in a human-verify checkpoint.

Purpose: This is where all five success criteria are demonstrated end-to-end. The offline tests in 03-02 prove the mechanics; this plan proves the real integration on qwen-35b and is the gate before Phase 4.
Output: scripts/verify_phase3.sh (e2e + criterion-2 latency + criterion-3 source-untouched + OBS-03 node_models assertion), scripts/cap_proof.py (criterion 4), tests/test_execute_integration.py (skippable integration tests), plus a human-verify checkpoint to run them on the live stack.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-openhands-execute/03-RESEARCH.md

# Mirror the launch/cleanup/port conventions of these proven Phase 1/2 scripts.
@scripts/verify_phase1.sh
@scripts/resume_real_data.sh
@orchestrator/main.py
@orchestrator/worker/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify_phase3.sh — live end-to-end proof (criteria 1, 2, 3 + OBS-03)</name>
  <files>scripts/verify_phase3.sh</files>
  <action>
    Write a bash script that proves criteria 1, 2, and 3 (plus OBS-03 node_models) against the live stack. Model launch/cleanup EXACTLY on scripts/verify_phase1.sh (proven): PORT=8099, EXIT-trap cleanup, lsof port-free wait, `.venv/bin/uvicorn` launched DIRECTLY (NEVER `uv run` — STATE.md 01-03 decision), --workers 1.

    Preconditions / guards:
    - TCP-probe localhost:4000 at the top; if down, echo "SKIP: LiteLLM :4000 down — start qwen-35b/LiteLLM and re-run" and exit 0 (skip, not fail — mirrors phase 1/2 skip semantics).
    - PORT=8099 (avoids 8000=35b, 8001=122b, 4000=litellm — STATE.md PORT decision). Use a fresh temp DATA_DIR (mktemp -d) so jobs.db/checkpoints.db are isolated; export DATA_DIR and EXECUTE_WORKSPACE_BASE=$tmpdir/langgraph-jobs so artifacts/workspaces are sandboxed and easy to inspect+clean.
    - EXIT trap: kill the server PID, `pkill -9 -f "uvicorn orchestrator.main:app"` as a backstop (STATE.md env-hygiene), wait for port 8099 free via lsof loop, rm -rf the temp dir. NO orphan left behind.

    Steps:
    1. SOURCE SNAPSHOT (criterion 3 — orchestrator source untouched): before launching, record a checksum manifest of the orchestrator source tree:
         `find orchestrator -name '*.py' -print0 | xargs -0 shasum > $tmpdir/src_before.sha`
       (W3 — DO NOT write a comment claiming this project is "NOT a git repo"; it IS a git repo on main. The shasum manifest is used because it is a portable, dependency-free byte-level check of the .py files and does not depend on the working tree's git state. Conceptually equivalent to `git diff --stat orchestrator/` showing no changes, but shasum is what we assert on. If you add a code comment, say "shasum manifest = portable byte-level check; cf. git diff --stat orchestrator/" — do NOT claim there is no git repo.)
    2. Launch: `EXECUTE_MAX_ITERATIONS=20 EXECUTE_TIMEOUT_SECS=900 "$PROJECT_DIR/.venv/bin/uvicorn" orchestrator.main:app --workers 1 --port 8099 --log-level info >> $tmpdir/uvicorn.log 2>&1 &` ; capture SERVER_PID. (Do NOT set ORCHESTRATOR_TEST_GRAPH — we want the REAL graph.) Poll GET /health until 200 (up to ~30s).
    3. Submit a simple deterministic goal: `curl -s -X POST http://localhost:8099/goals -H 'Content-Type: application/json' -d '{"goal":"Create a file named hello.txt containing the text hello world, then finish."}'` → extract job_id. Assert HTTP path returned a job_id (criterion 1 start).
    4. STATUS-TRANSITION + CRITERION-2 LATENCY PROBE (the heart of this script): poll GET /jobs/{job_id}/status in a loop (up to ~600s, sleep 2s between polls). For EACH poll use `curl -s -o /tmp/st.json -w '%{time_total}' .../status` and assert time_total < 1.0 (sub-second). Record the set of distinct statuses observed. CRITICALLY: while status==EXECUTING, do an extra burst of 5 rapid status calls and assert every one returns < 1.0s — this is the concrete "event loop not blocked during OpenHands run" proof (criterion 2). Break when status in {DONE, FAILED}.
    5. Assert criterion 1: the observed status sequence included PLANNING and EXECUTING and ended at DONE (FAILED is a soft-fail — record it but the criterion wants DONE; if 35B flakes to FAILED, note it and let the human-verify checkpoint judge). No approval gate was ever required (the pipeline ran straight through).
    6. Assert criterion 3 (artifacts): EXECUTE_WORKSPACE_BASE/<job_id>/workspace/ exists and contains research.md, plan.md, and execution_transcript.jsonl. Print their sizes. (execution_summary.md too.)
    7. Assert criterion 3 (source untouched): recompute `find orchestrator -name '*.py' -print0 | xargs -0 shasum > $tmpdir/src_after.sha` and `diff $tmpdir/src_before.sha $tmpdir/src_after.sha` → MUST be empty. Print "[PASS] orchestrator source unmodified" or fail loudly.
    8. Assert OBS-03 (I3 — node_models.execute): fetch the job result and assert the execute model was recorded. Use the EXACT string execute_node writes — per 03-02 it is `node_models={"execute": os.getenv("EXECUTE_MODEL","openai/qwen-35b")}`, and .env sets EXECUTE_MODEL=openai/qwen-35b, so the recorded value is "openai/qwen-35b" (WITH the openai/ prefix — match it exactly, not bare "qwen-35b"):
         `curl -s http://localhost:8099/jobs/$job_id/result | jq -e '.node_models.execute == "openai/qwen-35b"'`
       (If the result endpoint path/shape differs — confirm against orchestrator/main.py — use the actual route, e.g. GET /jobs/{id}/result returning the final state; the assertion target is node_models.execute == "openai/qwen-35b". If node_models is nested under a "state" key in the response, adjust the jq path accordingly but keep the equality target.) Print "[PASS] OBS-03: node_models.execute == openai/qwen-35b" or fail loudly.
    9. Print a clear PASS/FAIL summary block listing criterion 1/2/3 + OBS-03 outcomes (like verify_phase1.sh's final summary).

    Make the script `set -euo pipefail` where safe, but allow the poll loop to tolerate transient curl failures.
  </action>
  <verify>
    `bash -n scripts/verify_phase3.sh` (syntax check) passes.
    `grep -q "\.venv/bin/uvicorn" scripts/verify_phase3.sh` and `grep -q "uv run" scripts/verify_phase3.sh` → first succeeds, second returns NOTHING (never uv run).
    `grep -q "8099" scripts/verify_phase3.sh` (non-colliding port) and `grep -q "pkill -9 -f" scripts/verify_phase3.sh` (orphan backstop) succeed.
    `grep -q "time_total" scripts/verify_phase3.sh` (criterion-2 latency assertion) and `grep -q "shasum" scripts/verify_phase3.sh` (criterion-3 source manifest) succeed.
    W3 — no wrong "not a git repo" comment: `grep -qi "not a git repo\|NOT a git repo" scripts/verify_phase3.sh` returns NOTHING.
    I3 — OBS-03 assertion present with the exact prefixed model string: `grep -q "node_models.execute" scripts/verify_phase3.sh` AND `grep -q "openai/qwen-35b" scripts/verify_phase3.sh` both succeed.
    Live run (in the checkpoint, when :4000 is up): the script prints PASS for criteria 1/2/3 + OBS-03 (or self-skips when :4000 is down).
  </verify>
  <done>
    scripts/verify_phase3.sh launches the real service on :8099 via .venv/bin/uvicorn with no orphan, submits a goal, proves the status walks PLANNING→EXECUTING→DONE with no approval gate, asserts every status poll (including a burst during EXECUTING) returns < 1s, asserts research.md/plan.md/execution_transcript.jsonl exist in the per-job workspace, asserts the orchestrator source shasum manifest is unchanged (no incorrect "not a git repo" comment), and asserts node_models.execute == "openai/qwen-35b" via the result endpoint (OBS-03). Self-skips cleanly when :4000 is down.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/cap_proof.py (criterion 4) + tests/test_execute_integration.py (skippable integration)</name>
  <files>scripts/cap_proof.py, tests/test_execute_integration.py</files>
  <action>
    Two artifacts proving the max-iterations cap and providing a pytest harness.

    A) scripts/cap_proof.py — criterion 4 (EXEC-04): a would-be-infinite execute terminates at the cap.
       - TCP-probe :4000; if down, print "SKIP: LiteLLM :4000 down" and sys.exit(0).
       - Set EXECUTE_MAX_ITERATIONS to a tiny value (e.g. "2") via os.environ BEFORE importing the adapter, and EXECUTE_TIMEOUT_SECS to a bounded value (e.g. "300").
       - Call run_openhands directly (the synchronous adapter — no need for the full graph) with a goal engineered to NOT finish quickly, e.g. goal="Keep listing the current directory and counting files forever; never call finish." plan="" workspace_dir=mktemp, max_iterations=2.
       - Assert the returned dict's execution_status is "partial" (mapped from SDK ERROR/STUCK at cap) OR "failed", and iterations_used <= 2 (cap respected), and final_sdk_status in {"error","stuck","timeout"} — i.e. it TERMINATED rather than looping. Print the result dict. Exit 0 on success, nonzero on assertion failure.
       - This proves the cap returns a terminal status without running forever. (RESEARCH Pattern 4: at cap, run() sets ERROR and returns — does not raise.)

    B) tests/test_execute_integration.py — pytest harness, skippable when :4000 is down (RESEARCH Pattern 7).
       - Reuse the _litellm_is_up TCP probe pattern from tests/test_real_graph.py. Decorate the live test with @pytest.mark.skipif(not _litellm_is_up(), reason="LiteLLM :4000 down").
       - test_full_pipeline_to_done (live): use fastapi.testclient.TestClient(app) from orchestrator.main; set a temp DATA_DIR + EXECUTE_WORKSPACE_BASE via monkeypatch/env BEFORE constructing the client; POST /goals with the hello.txt goal; poll GET /jobs/{id}/status (each poll asserts the call itself returns quickly — use time.perf_counter around client.get and assert < 1.0s as the offline-ish criterion-2 analogue); assert final status == "DONE" (allow up to ~600s); assert workspace research.md/plan.md/execution_transcript.jsonl exist (note: execution_transcript.jsonl is written by run_openhands on ALL paths per 03-01, so it exists even if the agent errored). NOTE: TestClient runs the app in-process; this is a convenience harness — the authoritative criterion-2 proof is the out-of-process verify_phase3.sh in Task 1.
       - test_execute_stub_offline (always runs): set ORCHESTRATOR_TEST_GRAPH=1 + temp DATA_DIR; TestClient; POST a stub goal; poll until DONE within ~15s; assert DONE. This guarantees the offline path still completes end-to-end through the API.
       - Add a module note that the live test is intentionally skipped in CI / when :4000 is down and is the slow integration gate.
  </action>
  <verify>
    `.venv/bin/python -c "import ast; ast.parse(open('scripts/cap_proof.py').read()); print('cap_proof parses')"` succeeds; `grep -q "EXECUTE_MAX_ITERATIONS" scripts/cap_proof.py` and `grep -q "iterations_used" scripts/cap_proof.py` succeed.
    `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest tests/test_execute_integration.py::test_execute_stub_offline -q` passes (offline path green).
    `grep -q "skipif" tests/test_execute_integration.py` succeeds (live test is skippable).
    Full offline suite still green: `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest -q` passes (live integration test is skipped when :4000 down, not failed).
    Live (in checkpoint, :4000 up): `.venv/bin/python scripts/cap_proof.py` prints a terminal result with iterations_used ≤ cap and exit 0 (or self-skips).
  </verify>
  <done>
    scripts/cap_proof.py proves an unbounded-looking execute terminates at a tiny iterations cap with a terminal partial/failed status (criterion 4), self-skipping when :4000 is down. tests/test_execute_integration.py adds a skippable live full-pipeline-to-DONE test plus an always-green offline stub-pipeline test; the offline suite stays green with the live test skipped when the stack is down.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 3 stack is wired and three proofs exist:
    - scripts/verify_phase3.sh — live e2e: PLANNING→EXECUTING→DONE, sub-second status during EXECUTING (criterion 2), workspace artifacts (research.md/plan.md/execution_transcript.jsonl), orchestrator source unmodified, node_models.execute == openai/qwen-35b (criteria 1, 2, 3 + OBS-03).
    - scripts/cap_proof.py — max-iterations cap → terminal status, no infinite loop (criterion 4).
    - tests/test_execute_integration.py — skippable live full-pipeline-to-DONE + always-green offline stub pipeline.
    These require the LIVE qwen-35b + LiteLLM :4000 stack and a multi-minute 35B run, which is why human eyes confirm the actual run.
  </what-built>
  <how-to-verify>
    Ensure LiteLLM :4000 and qwen-35b (mlx on :8000) are up. Then from /Users/ohama/projs/LangGraph_OpenHands:

    1. Offline regression (must be green, SDK/network-free):
       `ORCHESTRATOR_TEST_GRAPH=1 .venv/bin/python -m pytest -q`
       Expect: all tests pass; live integration test SKIPPED; no openhands banner / litellm warnings polluting output.

    2. Criterion 4 (cap) — fast (~1-3 min):
       `.venv/bin/python scripts/cap_proof.py`
       Expect: prints a result dict with execution_status partial|failed, iterations_used ≤ 2, final_sdk_status error|stuck|timeout, exit 0. (Proves no infinite loop.)

    3. Criteria 1/2/3 + OBS-03 — slow (up to ~10 min for a 35B run):
       `bash scripts/verify_phase3.sh`
       Expect a final summary with:
         [PASS] criterion 1: status PLANNING→EXECUTING→DONE, no approval gate
         [PASS] criterion 2: all status polls (incl. burst during EXECUTING) < 1s — event loop not blocked
         [PASS] criterion 3: research.md/plan.md/execution_transcript.jsonl in ~/projs/langgraph-jobs/<job_id>/workspace AND orchestrator source shasum unchanged
         [PASS] OBS-03: node_models.execute == openai/qwen-35b
       Confirm NO orphan uvicorn remains: `pgrep -fl "uvicorn orchestrator.main:app"` returns nothing after the script exits.

    Note: if 35B flakes (XML tool-call failure) and the job ends FAILED instead of DONE, that is a known 35B reliability risk (RESEARCH Pitfall 3), not a wiring bug — re-run once; the cap/timeout still contained it. Judge whether DONE was achieved on a clean run.
  </how-to-verify>
  <resume-signal>Type "approved" if criteria 1-4 + OBS-03 pass on the live run (DONE reached, sub-second status, artifacts present, source untouched, node_models.execute==openai/qwen-35b, cap terminates), or describe which criterion failed and the observed output.</resume-signal>
</task>

</tasks>

<verification>
- Criterion 1 (ORCH-01): verify_phase3.sh shows PENDING→RESEARCHING→PLANNING→EXECUTING→DONE with no approval gate.
- Criterion 2 (EXEC-02): every status poll, including a rapid burst while EXECUTING, returns < 1s (curl %{time_total}).
- Criterion 3 (EXEC-03/OBS-02): per-job workspace under ~/projs/langgraph-jobs/<job_id>/ holds research.md/plan.md/execution_transcript.jsonl; orchestrator source shasum manifest unchanged before/after.
- Criterion 4 (EXEC-04): cap_proof.py shows iterations_used ≤ cap, terminal partial/failed status, no infinite loop.
- OBS-03: verify_phase3.sh asserts node_models.execute == "openai/qwen-35b" via GET /jobs/{id}/result (inspectable also via workspace execution_summary.md).
- No-orphan launch on :8099 via .venv/bin/uvicorn; pkill backstop; offline suite green with live tests skipped when :4000 down.
</verification>

<success_criteria>
- All five Phase 3 success criteria demonstrated on the live stack and human-confirmed.
- Service launches/cleans up with no orphan on a non-colliding port.
- Offline suite remains green and SDK/network-free; live proofs are skippable when the stack is down.
</success_criteria>

<output>
After completion (post-approval), create `.planning/phases/03-openhands-execute/03-03-SUMMARY.md`.
Record: the live run's status sequence + max status-poll latency observed during EXECUTING, the workspace artifact sizes, the source-untouched diff result, the node_models.execute value asserted, the cap_proof iterations_used/final_sdk_status, whether the run reached DONE or flaked to FAILED (and if re-run was needed), and confirmation of no orphan uvicorn.
</output>
